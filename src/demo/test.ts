export const app = `import {Button} from '@nextui-org/react'
export default function App() {
  return (
    <h1 className="text-blue-500">
      wei
      <Button>yu</Button>
    </h1>
  )
}
`;

export const react = {
  "./App.tsx": app,
};

export default react;
